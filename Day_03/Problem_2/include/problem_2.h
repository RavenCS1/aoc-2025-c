#ifndef PROBLEM_2H
#define PROBLEM_2H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

constexpr int MAX_LENGTH_OF_LINE = 102; // 101 + 1
constexpr int NUMBER_OF_LINES = 200;
constexpr int NUMBER_OF_PAIRS = 6; // 6 * 2 = 12

int counting_lines(FILE [restrict const static 1]);
long summing(char [restrict const static MAX_LENGTH_OF_LINE], bool [restrict const static MAX_LENGTH_OF_LINE - 2]);

#endif
